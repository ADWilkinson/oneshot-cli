{
  "name": "oneshot-ship",
  "version": "2.0.0",
  "description": "Fire-and-forget agentic software runtime: detach a task, get a reviewed PR plus a proof-of-work receipt.",
  "homepage": "https://adwilkinson.github.io/oneshot-cli",
  "type": "module",
  "bin": {
    "oneshot": "src/cli.ts"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target bun",
    "check": "bun run typecheck && bun test && bun run docs:check",
    "docs:check": "node scripts/check-docs.mjs",
    "dev": "bun run src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": [
    "cli",
    "ai",
    "agentic-workflows",
    "automation",
    "codex",
    "claude",
    "mcp",
    "ssh",
    "bun",
    "developer-tools",
    "code-review",
    "pull-request"
  ],
  "author": "ADWilkinson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ADWilkinson/oneshot-cli.git"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "files": [
    "src/**/*.ts",
    "!src/**/*.test.ts",
    "prompts",
    "skills",
    "CHANGELOG.md"
  ]
}
