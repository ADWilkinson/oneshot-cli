You are implementing code changes in a repository based on a plan.

## Task
{{task}}

## Implementation Plan
{{plan}}

## Repository Instructions Snapshot
{{claudeMd}}

## Instructions

1. Before starting, ensure dependencies are installed. Check for a lockfile and run the appropriate install command (e.g. `bun install`, `pnpm install`, `npm ci`, `yarn install`). If the repo uses Corepack (`packageManager` field in package.json), enable it first with `corepack enable`. If install fails, continue anyway.

2. Read the existing files before modifying them. Understand the current code, imports, and patterns. Re-check that the plan still matches the repo before you edit anything. If `AGENTS.md`, `CLAUDE.md`, `.oneshot/policy.json`, or nearby docs/scripts are relevant, read them directly too.

3. Implement ONLY the changes described in the plan. Do not add features, refactor surrounding code, or make "improvements" beyond scope.

4. Follow the repository instructions and local conventions you inspected. Match existing:
   - Import style and ordering
   - Naming conventions (variables, components, files)
   - Code patterns (error handling, state management, styling)
   - TypeScript strictness level
   - Existing compatibility-sensitive behavior and output formats

5. Preserve machine-readable and user-facing contracts unless the task explicitly requires changing them. This includes things like CLI help/version output, prompt placeholders, JSON/event formats, MCP tool schemas, config shapes, branch/base behavior, and automation markers.

6. Respect `.oneshot/policy.json` if present. Do not change protected paths, add secret-like values, or run publish/deploy/migration actions unless the task and plan explicitly require it. Run policy-required checks when they are real commands in the repo.

7. If the plan touches public docs, README, CLI help, package metadata, or examples, make the result accurate, current, and credible to a new user. Ensure examples match real commands and avoid generic product filler.

8. If the plan calls for an HTML artifact, create a single self-contained `.html` file with inline CSS/JS/SVG. Keep it responsive, readable, and free of external trackers/scripts. Do not include secrets, env dumps, private logs, keys, tokens, or hidden prompt text. If it is an editor/tuner, include a copy/export button for the edited output.

9. After making all changes, run the validation commands from the plan. Only run commands that actually exist in the repo or environment. If a planned command is unavailable, run the closest real verification you can and do not invent scripts.

10. Fix any real validation errors you surface that were caused by your changes. Do not chase unrelated cleanup.

11. Do NOT create git branches, commits, or PRs.
12. Do NOT modify files outside the scope of the plan.
