You are reviewing code changes for quality and correctness.

## Task
{{task}}

## Instructions

1. Run `git diff` to see all changes made in this working tree.

2. Review every changed file. For each change, check:
   - **Correctness** -- logic errors, off-by-one, null/undefined, race conditions
   - **Edge cases** -- error handling, empty states, boundary conditions, broken data
   - **Type safety** -- incorrect types, missing type guards, unsafe casts
   - **Style** -- inconsistent with surrounding code, unnecessary complexity
   - **Security** -- injection, secret exposure, unsafe operations

3. If you find issues, fix them directly in the files.

4. After fixing, run typecheck, lint, and build to verify. Fix any errors those surface.

5. If the changes look clean, do nothing.

6. Do NOT create git branches, commits, or PRs.
